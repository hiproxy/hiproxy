{
  "name": "hiproxy",
  "version": "1.0.1",
  "description": "hiproxy is a Node.js proxy client",
  "main": "src/index.js",
  "bin": {
    "hiproxy": "bin/cli.js",
    "hip": "bin/cli.js"
  },
  "scripts": {
    "seq": "mocha test/seq/seq.test.js",
    "mocha": "mocha test/**/*.test.js --no-color",
    "cover": "istanbul cover _mocha test/**/*.test.js --no-color",
    "test": "npm run cover -- --report lcovonly --no-color && cat ./coverage/lcov.info | coveralls"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/zdying/hiproxy.git"
  },
  "keywords": [
    "proxy",
    "proxy",
    "client"
  ],
  "author": "zdying",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/zdying/hiproxy/issues"
  },
  "homepage": "https://github.com/zdying/hiproxy#readme",
  "dependencies": {
    "colors": "^1.1.2",
    "hemsl": "^1.0.7",
    "simple-mime": "^0.1.0"
  },
  "devDependencies": {
    "coveralls": "^2.11.16",
    "istanbul": "^0.4.5",
    "mocha": "^3.2.0",
    "mocha-lcov-reporter": "^1.2.0",
    "request": "^2.79.0"
  }
}
